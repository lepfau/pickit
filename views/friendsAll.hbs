<div>
    <h1> Your friends</h1>
    <i class="fas fa-plus-circle"></i>
</div>

<div>
{{#each} friends}
    <p>{{firstName}} {{lastName}}</p>
    <img src="{{image}}" alt="{{firstName}} {{lastName}}">
{{/each}}
</div>